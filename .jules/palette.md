## 2024-05-21 - Dynamic CLI Formatting Pattern
**Learning:** For CLI applications, raw dictionary outputs are poor UX. Implementing a dynamic formatter that calculates padding based on the longest key ensures clean alignment. Using a themed color scheme (Red for health/risks, Yellow for currency, Green for progress) helps users scan information quickly.
**Action:** Use the `exibir_painel` pattern (with hierarchical support and empty input safety) for all structured data displays in CLI environments.
