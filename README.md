# Jogo-RPG-de-Ca-que

Jogo RPG de Caíque: Estratégia Militar Avançada ​Simulação estratégica profunda. Gerencie a MilitaryBase, recursos (Ouro, Éter) e tecnologia ramificada. O AIProtocol oferece análises de risco. Recrute Characters com habilidades detalhadas e Afinidade (Harém/Romance) para aprimorar a Força Bélica. Seinen/Fantasia/Drama.

## 🛡️ Setup

This project requires Python 3 and `pip`.

### 1. Install Dependencies

```bash
pip install -r requirements.txt
```
*Note: a `requirements.txt` file will be created in the next step.*

### 2. Create `.env` file
Copy the `.env.example` file to a new file named `.env`:
```bash
cp .env.example .env
```
Open the `.env` file and replace `your_secret_password_here` with a secure password of your choice. This is your `SENHA_BASE`.

### 3. Run the game
```bash
python3 supremo_rpg_final.py
```
